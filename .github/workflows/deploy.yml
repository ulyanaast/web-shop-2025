name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up deployment
        run: |
          # Создаем структуру
          mkdir -p public
          
          # Копируем ВСЁ содержимое frontend
          cp -r frontend/* public/
          
          # Явно копируем cart.html в корень (дублируем)
          cp frontend/templates/cart.html public/
          
          # Проверяем
          echo "Содержимое public:"
          ls -la public/
          echo "Проверка cart.html:"
          ls -la public/cart.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: 'public'

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
